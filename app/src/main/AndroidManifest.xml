<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PokedexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokedex"
        android:networkSecurityConfig="@xml/network_security_config">

        <!--
            Single Activity Architecture:
            MainActivity is the ONLY Android Activity in the app. All screens
            (list / detail / etc.) are Composable "destinations" hosted inside
            a single NavHost (see presentation/navigation/PokedexNavGraph.kt).
            This is the Compose-era equivalent of the classic
            "one Activity + many Fragments" pattern.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Pokedex">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
